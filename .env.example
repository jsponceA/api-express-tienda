# Application
NODE_ENV=development
PORT=3000

# API URL (solo para producción, para documentación Swagger)
# API_URL=https://api.tudominio.com
# API_URL=http://tu_ip_vps:3000

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tienda_db
DB_USERNAME=postgres
DB_PASSWORD=postgres

# PRODUCCIÓN:
# NODE_ENV=production
# API_URL=https://api.tudominio.com
# Usa contraseñas seguras y diferentes a las de desarrollo
# Nunca subas el archivo .env al repositorio (ya está en .gitignore)
